# $schema: https://json.schemastore.org/github-workflow
name: 'Build and release plugin (dev)'

on:
  pull_request:
    branches:
      - jellyfin-10.9.x
    paths-ignore:
      - '**/*.md'
      - '**/*.yml'
      - '**/*.yaml'
  workflow_dispatch:

env:
  DOTNET_VERSION: "8.0"
  REPO_NAME: "jellyfin-dev"

jobs:
  test:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/test.yaml@master
    with:
      dotnet-version: ${{ env.DOTNET_VERSION }}

  build:
    uses: lyarenei/jellyfin-plugin-listenbrainz/.github/workflows/build.yml@main

  upload-repo:
    if: ${{ contains(github.repository, 'lyarenei/') }}
    needs: build
    uses: lyarenei/jellyfin-plugin-listenbrainz/.github/workflows/upload-repo.yml@main
    with:
      repo: ${{ env.REPO_NAME }}
    secrets:
      host: ${{ secrets.DEPLOY_HOST }}
      user: ${{ secrets.DEPLOY_USER }}
      key: ${{ secrets.DEPLOY_KEY }}

  update-manifest:
    if: ${{ contains(github.repository, 'lyarenei/') }}
    needs: upload-repo
    uses: lyarenei/jellyfin-plugin-listenbrainz/.github/workflows/publish.yml@main
    with:
      repo: ${{ env.REPO_NAME }}
    secrets:
      host: ${{ secrets.DEPLOY_HOST }}
      user: ${{ secrets.DEPLOY_USER }}
      key: ${{ secrets.DEPLOY_KEY }}
